import { PlayerInfo } from "../types/type";

export const playerStatus: PlayerInfo = ["broken", "bust", "stand", "surrender"];
export const playerActions: PlayerInfo = ["bet", "surrender", "stand", "hit", "double"];
